import { pool } from "../config/db.js";


//ADD USERS
const addUserQuery = async(user) => {
    try {

    } catch (error) {
        console.log(`Erro code: ${error.code}, Error message: ${error.message}`);
    }
};